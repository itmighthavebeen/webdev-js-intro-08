// Prevent us from attempting to use variables
// that are not declared
"use strict"
/*// Create an array
const numbers = [8.2,5.6,3.4,7.6];

// array using a basic for loop
for (let i = 0; i < numbers.length; i++) {
    console.log(numbers[i]);  // Logs each value of the array
}
// Create an array
const streets = ["Wheeler", "Turner", "Broadway", "Boardwalk", "Atlantic"];

// Array- for...of syntax
for (const string of streets) {
    console.log(string);  // write out and check
}
// Create an array
const tolerances = [1.2, 0.32, 2.653, .04, 1.55];

//  Array.forEach() method
tolerances.forEach(num => {
    console.log(num);  // Logs each value of the array
});

//try to modify for array-NOTE DOESN'T DO A THING TO VALUE :(
// Array- for...of syntax
let place = 0;
for (const string of streets) {
   // string[place] = string.concat("This is a street called ", string);
    console.log(string);  // write out and check
    place++;
}

//  Try to change values in forEach Array.forEach() method
tolerances.forEach(num => {
    num = num * 2;
    console.log(num);  // Logs each value of the array
});
//looked like it was working but when checked array - values remain unchanged :(
console.log(tolerances);
//
//
//The for...in statement iterates a specified variable over all the enumerable
// properties of an object. For each distinct property, JavaScript executes the
//  specified statements. 
//
const birdInfo = {fname:"Junco", family:"American Sparrows", nickname:"snowbirds"};

let index = "";
for (let i in birdInfo) {
  index += birdInfo[i];
  console.log(`Index: ${index}, Value: ${birdInfo[index]}`);
// console.log(text); 
} */




